/* global describe, it */

var expect = require('chai').expect
var Action = require('../lib/action')

describe('Action', () => {
  describe('#create()', () => {
    it('should return an action object', (done) => {
      done()
    })
  })
})
